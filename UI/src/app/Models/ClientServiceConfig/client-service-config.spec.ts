import { ClientServiceConfig } from './client-service-config';

describe('ClientServiceConfig', () => {
  it('should create an instance', () => {
    expect(new ClientServiceConfig()).toBeTruthy();
  });
});
