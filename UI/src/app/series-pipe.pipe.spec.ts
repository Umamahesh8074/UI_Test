import { SeriesPipePipe } from './series-pipe.pipe';

describe('SeriesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SeriesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
