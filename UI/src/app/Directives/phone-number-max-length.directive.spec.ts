import { PhoneNumberMaxLengthDirective } from './phone-number-max-length.directive';

describe('PhoneNumberMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
